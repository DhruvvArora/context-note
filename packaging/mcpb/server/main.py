# Placeholder only. The .mcpb manifest spec requires server.entry_point to
# point at a file inside the bundle, but the real server that actually runs
# is context_note.server, launched via the "command"/"args" in manifest.json
# using whichever interpreter has context-note installed. This file is never
# executed.
